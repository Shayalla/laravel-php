<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="stylesheet" href="/css/styles.css">
        <script src="/js/scripts.js"></script>

        <title>Laravel</title>
    </head>
    <body class="antialiased">
        <h1 class="title">Jéssica</h1>
        {{-- <p>{{ $nome }}</p>
        @if($nome == "Pedro")
            <p>0 nomeéPedro</p>
        @elseif ($nome == "Matheus")
            <p>0 nome é {{ $nome }} e ele tem {{ $idade }} anos, e trabalha como {{ $profissao }} </p>
        @else
            <p>0 nome não é Pedro</p>
        @endif
            @for ($i = 0; $i < count($arr); $i++)
                <p>{{ $arr[$i] }}</p>
        @endfor
        @foreach($nomes as $nome)
            <p>{{ $loop->index }}</p>
            <p>{{ $nome }}</p>
        @endforeach --}}
    </body>
</html>
